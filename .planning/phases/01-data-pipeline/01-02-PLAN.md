---
phase: 01-data-pipeline
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/lib/schedule-hash.ts
  - tests/schedule-hash.test.ts
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "computeScheduleHash produces the RESEARCH.md-verified 8-hex-char hash 'ffe3f098' for game ids [401628355, 401628301, 401628288] regardless of input array order"
  artifacts:
    - scripts/lib/schedule-hash.ts
    - tests/schedule-hash.test.ts
  key_links:
    - "computeScheduleHash output -> games.json top-level scheduleHash field, written by Plan 05's orchestration script"
    - "computeScheduleHash's numeric sort order -> must match the exact ordering Phase 8's share-link bitpack index will later use (documented here so it isn't silently changed downstream)"
---

<objective>
Implement `computeScheduleHash` — the pure function that fingerprints a season's game-id list per D-11/D-12 — via a RED-GREEN-REFACTOR cycle, using the exact fixture values verified during research.

Purpose: `games.json`'s `scheduleHash` field is how stored scenarios and share links (Phase 8) detect a stale schedule. Getting the sort order and truncation exactly right here is load-bearing for every later phase that trusts this fingerprint.
Output: `scripts/lib/schedule-hash.ts` exporting `computeScheduleHash`, fully covered by `tests/schedule-hash.test.ts`.
</objective>

## Artifacts this phase produces

- `scripts/fetch-data.ts` — CLI entrypoint (`pnpm fetch-data <season>`) [Plan 05]
- `scripts/lib/schemas.ts` — `RawTeamSchema`, `RequiredTeamFieldsSchema`, `transformTeam`, `reportRequiredFieldFailures`, `RawGameSchema`, `transformGame` [Plan 03]
- `scripts/lib/schedule-hash.ts` — `computeScheduleHash` [this plan]
- `scripts/lib/coverage.ts` — `vendorLogo`, `buildCoverageReport` [Plan 04]
- `public/data/{season}/teams.json`, `public/data/{season}/games.json`, `public/data/{season}/coverage.json` [Plan 05]
- `public/logos/{teamId}.png` (vendored, one per FBS team) [Plan 05]
- `public/logos/placeholder.svg` [Plan 04]
- `tests/schedule-hash.test.ts` [this plan], `tests/schemas.test.ts` [Plan 03], `tests/coverage.test.ts` [Plan 04]
- `tests/fixtures/cfbd-teams-sample.json`, `tests/fixtures/cfbd-games-sample.json` [Plan 01]
- `vitest.config.ts`, `tsconfig.scripts.json`, `.env.example` [Plan 01]
- `package.json` script entries: `fetch-data`, `test`, `typecheck:scripts` [Plan 01]

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-pipeline/01-CONTEXT.md
@.planning/phases/01-data-pipeline/01-RESEARCH.md
@.claude/CLAUDE.md

This plan runs in parallel with 01-03 and 01-04 (all depend only on 01-01, touch disjoint files). No SUMMARY reference to 01-01 is needed beyond the fixture file it created — read the fixture directly.
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Implement computeScheduleHash via RED-GREEN-REFACTOR</name>
  <files>scripts/lib/schedule-hash.ts, tests/schedule-hash.test.ts</files>
  <read_first>.planning/phases/01-data-pipeline/01-RESEARCH.md (Code Examples: scheduleHash computation; Pitfall 3: numeric vs lexicographic sort), .planning/phases/01-data-pipeline/01-CONTEXT.md (D-11, D-12), tests/fixtures/cfbd-games-sample.json</read_first>
  <behavior>
    - `computeScheduleHash([401628355, 401628301, 401628288])` returns exactly `'ffe3f098'` (8 lowercase hex characters) — the RESEARCH.md-verified value, produced by SHA-256 over the numerically-sorted, comma-joined ids, truncated to the first 8 hex characters (u32) per D-12.
    - `computeScheduleHash` is invariant to input array order: `computeScheduleHash([401628301, 401628355, 401628288])` equals `computeScheduleHash([401628355, 401628301, 401628288])` — the function must numerically sort internally before hashing, per D-11 and Pitfall 3 (never rely on caller-supplied order, never use the default lexicographic `.sort()`).
    - `computeScheduleHash(gameIds)` always returns a string matching `/^[0-9a-f]{8}$/` for any non-empty numeric array input.
    - `computeScheduleHash` is deterministic: two calls with the same input array (in any order) return the identical string.
    - `computeScheduleHash([])` returns a defined 8-character hex string without throwing — this is simply the hash of the empty joined-string input; do not special-case an empty array with a different code path.
  </behavior>
  <action>
    RED: create `tests/schedule-hash.test.ts` importing `computeScheduleHash` from `../scripts/lib/schedule-hash.ts` (module does not exist yet) and `describe`/`it`/`expect` from `'vitest'`; write the five cases above as `it` blocks. Run `pnpm vitest run tests/schedule-hash.test.ts` and confirm every test fails because the module cannot be resolved.

    GREEN: create `scripts/lib/schedule-hash.ts` exporting `computeScheduleHash(gameIds: number[]): string`. Implement using `node:crypto`'s `createHash('sha256')`: copy `gameIds` into a new array, sort the copy with the explicit numeric comparator `(a, b) => a - b` (never the default string-lexicographic sort — this is the exact bug documented in RESEARCH.md Pitfall 3), join the sorted array with `','`, feed the resulting string into the hash, and return `.digest('hex').slice(0, 8)`. Re-run the test file and confirm all five cases pass.

    REFACTOR: only if the sort/hash sequence reads unclearly — extract at most one small named helper, then re-run the tests to confirm they remain green. Do not add a CLI wrapper, logging, or file I/O to this module; it stays a pure function per RESEARCH.md's Recommended Project Structure.
  </action>
  <verify>
    <automated>pnpm vitest run tests/schedule-hash.test.ts</automated>
  </verify>
  <acceptance_criteria>`pnpm vitest run tests/schedule-hash.test.ts` exits 0 with all 5 tests passing; `grep -c "node:fs\|node:http" scripts/lib/schedule-hash.ts` reports 0, confirming this stays a pure function with no I/O; `grep -c "a - b" scripts/lib/schedule-hash.ts` reports at least 1, confirming the explicit numeric sort comparator is present (not the default lexicographic sort).</acceptance_criteria>
  <done>`computeScheduleHash` is implemented, exported, and every behavior case passes under Vitest, including the exact verified fixture assertion `[401628355, 401628301, 401628288] -> 'ffe3f098'`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed in this plan | `computeScheduleHash` is a pure function over an already-numeric, in-process array — no untrusted external input, no file/network I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | `computeScheduleHash` algorithm choice | low | accept | SHA-256 via `node:crypto` is fixed in code per D-12. This is a drift-detection fingerprint, not a secret-protection control (RESEARCH.md's Security Domain classifies this ASVS V6 usage as "Partial" — no confidentiality/integrity guarantee is claimed), so the residual risk of a theoretical hash-algorithm weakness is accepted as out of scope for this fingerprint's purpose. |
</threat_model>

<verification>
`pnpm vitest run tests/schedule-hash.test.ts` exits 0. `pnpm vitest run` (full suite, after Plans 03/04 land) continues to pass with this file included.
</verification>

<success_criteria>
`computeScheduleHash` correctly, deterministically fingerprints a sorted numeric game-id list to an 8-hex-char SHA-256 prefix, matching the RESEARCH.md-verified test vector, ready for Plan 05 to call from the live orchestration script.
</success_criteria>

<output>
Create `.planning/phases/01-data-pipeline/01-02-SUMMARY.md` when done
</output>
