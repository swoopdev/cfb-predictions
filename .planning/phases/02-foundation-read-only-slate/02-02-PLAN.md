---
phase: 02-foundation-read-only-slate
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - nuxt.config.ts
  - public/_redirects
autonomous: true
requirements: [FOUND-01, FOUND-03, SLATE-05]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ssr:false plus an explicit nitro.prerender.routes list of all 15 week paths is what makes a fresh browser tab opened directly at /week/N (not client-side-navigated to) resolve without a 404 on a static host"
    - "public/_redirects provides a Cloudflare Pages SPA-fallback rewrite (`/* /index.html 200`) as a backstop for any URL the prerender step didn't explicitly generate"
    - "No server/ directory, server route, or runtime API key/env var read exists anywhere in the built app — the entire app is static HTML/JS/JSON served from .output/public (FOUND-01)"
    - "A production pnpm build produces a static file for every one of weeks 1 through 15, including week 14 (which has zero games but must still resolve as a real route, not 404)"
  artifacts:
    - nuxt.config.ts (modified — ssr:false, nitro.prerender.routes)
    - public/_redirects
  key_links:
    - "nuxt.config.ts's nitro.prerender.routes list must stay in sync with app/pages/week/[week].vue's valid week range (currently weeks 1-15 fixed per D-16, 2026 season only)"
  prohibitions:
    - "Never add a server/ route, Nitro API handler, or any runtime env var read to nuxt.config.ts or app/ — this app has no backend in v1 (FOUND-01)"
    - "Never omit any week (1-15, including empty week 14) from nitro.prerender.routes — an omitted week 404s on direct/deep-link navigation despite working fine via client-side nav in pnpm dev"
---

<objective>
Configure the app for a fully static deploy: `ssr: false` (CLAUDE.md's locked v1 rendering mode), explicit Nitro prerender routes for all 15 week paths so direct/deep-link navigation to any `/week/N` URL resolves on a static host instead of 404ing, and a Cloudflare Pages SPA-fallback (`public/_redirects`) as a backstop.

Purpose: FOUND-01 (no backend) and FOUND-03 (zero network fetches for schedule data after initial load) are only true in production if the static build actually emits a real file per week route — `pnpm dev`'s client-side router will happily navigate anywhere, masking a build-time gap that only surfaces once deployed (RESEARCH.md Pitfall 1 / Open Question 1).
Output: `nuxt.config.ts` with `ssr: false` + `nitro.prerender.routes`, `public/_redirects`, and a verified `pnpm build` output containing a static file for every week 1-15.
</objective>

## Artifacts this phase produces

- `nuxt.config.ts` (modified) — `ssr: false`, `nitro.prerender.routes: ['/week/1', ..., '/week/15']` [this plan]
- `public/_redirects` — Cloudflare Pages SPA-fallback rewrite [this plan]

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/CLAUDE.md
@.planning/phases/02-foundation-read-only-slate/02-CONTEXT.md
@.planning/phases/02-foundation-read-only-slate/02-RESEARCH.md
@.planning/phases/02-foundation-read-only-slate/02-PATTERNS.md
</context>

<tasks>

<task type="auto" reversibility="costly — flipping ssr:false later requires re-testing every page/composable that assumes no server render exists; not a data-loss action, so rated costly rather than one-way">
  <name>Task 1: Configure static SPA build (ssr:false, prerender routes)</name>
  <files>nuxt.config.ts</files>
  <read_first>nuxt.config.ts (current), app/pages/week/[week].vue (confirms the route exists to prerender, created in 02-01), .planning/phases/02-foundation-read-only-slate/02-RESEARCH.md (Pitfall 1, Open Question 1, "Recommended Project Structure"), .planning/phases/02-foundation-read-only-slate/02-PATTERNS.md (nuxt.config.ts section), .claude/CLAUDE.md (Stack Patterns by Variant — `ssr: false` + `nuxt generate`)</read_first>
  <action>
    Modify `nuxt.config.ts`'s `defineNuxtConfig` call: add a top-level `ssr: false` (CLAUDE.md's locked v1 rendering mode — no Nitro server render, no server routes anywhere). Add `nitro: { prerender: { routes: [...] } }` where the array lists all 15 week paths explicitly and individually: `/week/1`, `/week/2`, `/week/3`, `/week/4`, `/week/5`, `/week/6`, `/week/7`, `/week/8`, `/week/9`, `/week/10`, `/week/11`, `/week/12`, `/week/13`, `/week/14`, `/week/15` — 14 is included even though it has zero games (D-15), since it must still resolve as a real static route on direct navigation, not just via the week-picker's client-side push. Do not rely solely on Nitro's `crawlLinks` default — a static host serves exactly the files Nitro emits, and the explicit list is the reliable mechanism (RESEARCH.md Pitfall 1). Keep the existing `routeRules: { '/': { prerender: true } }` unchanged. Do not add a `server/` directory, a Nitro API route, or any runtime env var read anywhere in this file or elsewhere in `app/` (FOUND-01).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `nuxt.config.ts` contains `ssr: false` at the top level of the `defineNuxtConfig` object
    - `nuxt.config.ts`'s `nitro.prerender.routes` array contains exactly 15 entries, `/week/1` through `/week/15` inclusive, with no gaps
    - `pnpm build` exits with status 0
    - No `server/` directory exists anywhere in the repository after the build (`ls server 2>/dev/null` finds nothing)
  </acceptance_criteria>
  <done>`pnpm build` completes successfully with `ssr: false` and all 15 week routes listed in `nitro.prerender.routes`.</done>
</task>

<task type="auto">
  <name>Task 2: Cloudflare SPA fallback + build-output verification</name>
  <files>public/_redirects</files>
  <read_first>public/ (directory listing — confirm no existing `_redirects` file), .planning/phases/02-foundation-read-only-slate/02-RESEARCH.md (Pitfall 1 — Cloudflare `_redirects` format)</read_first>
  <action>
    Create `public/_redirects` containing exactly one line: `/* /index.html 200` — Cloudflare Pages' SPA-fallback rewrite rule, so any URL not explicitly generated by Task 1's prerender step (a future query-param combination, or a week path if the explicit list is ever incomplete) still serves the SPA shell instead of a raw 404, satisfying SLATE-05's "linkable" requirement in production.

    Run `pnpm build` and inspect `.output/public/week/` to confirm every week 1-15 produced its own generated static output (RESEARCH.md's Open Question 1 verification step — the config alone is not sufficient evidence without checking the actual build artifacts). If any week directory/file under `.output/public/week/` is missing, Task 1's `nitro.prerender.routes` config did not take effect and must be revisited before this task is considered done.
  </action>
  <verify>
    <automated>pnpm build && ls .output/public/week</automated>
  </verify>
  <acceptance_criteria>
    - `public/_redirects` exists with exactly the content `/* /index.html 200` (no trailing directives, no other lines)
    - After `pnpm build`, `.output/public/week/` contains a subdirectory or file for each of weeks 1 through 15 — `ls .output/public/week` output includes all 15
  </acceptance_criteria>
  <done>A production build produces static output for all 15 week routes plus a working SPA fallback file; opening a fresh preview tab directly at any `/week/N` URL (not navigated to client-side) does not 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Static host (Cloudflare Pages) <-> browser | Serves exactly the files `.output/public` contains; anything not generated 404s unless `_redirects` catches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering / Denial of Service | Missing prerender route for a week path | low | mitigate | Task 2's explicit `ls .output/public/week` build-output check catches a config regression before deploy; `public/_redirects` is a second layer of defense for any URL the prerender step misses. |
| T-02-05 | Information Disclosure | `nuxt.config.ts` itself | low | accept | No secrets or API keys are ever referenced in this file — the CFBD key lives only in Phase 1's fetch-script `.env`, never bundled or read at runtime (FOUND-01). |

No `T-02-SC` row: this plan installs no new packages.
</threat_model>

<verification>
`pnpm build` exits 0; `.output/public/week/` contains all 15 week routes; `public/_redirects` exists with the exact single-line SPA-fallback content; no `server/` directory exists.
</verification>

<success_criteria>
The app is deployable as a pure static site: `ssr: false`, all 15 week routes prerendered, and a Cloudflare Pages SPA fallback in place — direct/deep-link navigation to any `/week/N` URL works in production, not just via client-side routing in `pnpm dev`.
</success_criteria>

<output>
Create `.planning/phases/02-foundation-read-only-slate/02-02-SUMMARY.md` when done
</output>
