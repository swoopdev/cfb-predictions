---
phase: 03-tiebreaker-engine
plan: 05
type: tdd
wave: 4
depends_on: ["03-03"]
files_modified:
  - tests/fixtures/tiebreakers/bigten.fixtures.ts
  - tests/tiebreakers-bigten.test.ts
autonomous: true
requirements: [TIE-01, TIE-02, TIE-03, TIE-04]

must_haves:
  truths:
    - "resolveConferenceChampionship('Big Ten', ...) returns the correct #1 and #2 seeds for hand-verified 2-, 3-, 4-, and 5-way Big Ten tie fixtures, matching the Big Ten's own published procedure (head-to-head, common opponents, next-highest-placed common opponent, cumulative opponent win pct with its explicit unbalanced-schedule clause)"
    - "A restart-vs-continue divergence fixture proves the two branches produce different champions, and the engine picks the one the Big Ten's actual restart rule (an eliminated team never returns) dictates"
    - "A Big Ten tie that bottoms out past cumulative-opponent-win-pct returns NeedsUserInput with reason.code === 'ranking-step', citing the SportSource Analytics Team Rating Score step, not a guessed winner"
  artifacts:
    - tests/fixtures/tiebreakers/bigten.fixtures.ts
    - tests/tiebreakers-bigten.test.ts
  key_links:
    - "bigten.fixtures.ts's hand-derived Game/outcome literals are the only ground truth this test file trusts -- every expected result must be independently hand-computed against PITFALLS.md's Big Ten step text, not reverse-engineered from whatever the engine happens to output"
---

<objective>
Prove TIE-01 through TIE-04 for the Big Ten specifically: a full, hand-verified fixture matrix (per D-10) exercising 2-, 3-, 4-, and 5-way ties, a restart-vs-continue divergence, a partial head-to-head graph, and a zero-common-opponents case, asserting full trace content (per D-12) for every case that bottoms out at NeedsUserInput.

Purpose: this is one of four conference-specific correctness gates the phase's success criteria demand. The Big Ten's primary PDF was re-fetched and re-verified verbatim during this phase's own research (HIGH confidence) -- these fixtures are the concrete proof that verbatim text was translated correctly into code, including the explicit "eliminated team shall not be pulled back into the tiebreaker for any future step(s)" and "if all teams involved in the tie are tied after any step, all teams will continue to the next step" clauses.
Output: `tests/fixtures/tiebreakers/bigten.fixtures.ts` and `tests/tiebreakers-bigten.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tiebreaker-engine/03-CONTEXT.md
@.planning/phases/03-tiebreaker-engine/03-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-derived Big Ten fixture matrix</name>
  <files>tests/fixtures/tiebreakers/bigten.fixtures.ts</files>
  <read_first>
    .planning/research/PITFALLS.md (section "Big Ten -- official tiebreaker document" -- the exact ordered steps A.1-7 and B's governing restart clauses, verbatim; already loaded in this conversation's context above)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-03, D-10, D-12)
    public/data/2026/teams.json (real Big Ten team ids for readable, grounded fixtures)
    shared/domain/tiebreakers/types.ts (Game, TeamId, GameId shapes from Plans 03-01/03-02/03-03)
  </read_first>
  <action>
    Create `tests/fixtures/tiebreakers/bigten.fixtures.ts` following the same `.ts` fixture-module format as sec.fixtures.ts (Plan 03-04) -- typed `Game[]`/outcome-map literals with inline hand-derivation comments. Use real Big Ten team ids for readability (Big Ten roster: Illinois 356, Indiana 84, Iowa 2294, Maryland 120, Michigan 130, Michigan State 127, Minnesota 135, Nebraska 158, Northwestern 77, Ohio State 194, Oregon 2483, Penn State 213, Purdue 2509, Rutgers 164, UCLA 26, USC 30, Washington 264, Wisconsin 275). Construct and hand-verify, with an inline comment showing the arithmetic per team at each relevant step, for the same 8-case matrix as sec.fixtures.ts:
    1. `bigTenTwoWayTie` -- 2 teams tied for the same spot, resolved by head-to-head.
    2. `bigTenThreeWayTie` -- 3 teams tied, resolved partway through the procedure, exercising a genuine restart.
    3. `bigTenFourWayTie` -- 4 teams tied, with at least one step producing a 2-2 split.
    4. `bigTenFiveWayTie` -- 5 teams tied, deep enough to exercise 2+ restart cycles.
    5. `bigTenRestartVsContinueDivergence` -- constructed so incorrectly continuing (instead of restarting) after a partial separation would produce a DIFFERENT #1/#2 pair than correct behavior; the comment must state both answers and which is correct per the Big Ten's explicit "revert to the beginning of the applicable tiebreaker procedures" clause.
    6. `bigTenPartialHeadToHeadGraph` -- 3 tied teams where not every pair played each other, exercising the Big Ten's explicit "(a) one team beat all others -> removed, revert to beginning; (b) no team beat all others -> next step" clause literally, not a naive sort.
    7. `bigTenZeroCommonOpponents` -- 2+ tied teams with zero conference opponents in common (the Big Ten's own published 2025 scenario sheet shows exactly this case per RESEARCH.md/PITFALLS.md, so this is a documented real occurrence, not a hypothetical) -- verify the walk falls through cleanly, never a NaN.
    8. `bigTenUnbalancedScheduleAndNeedsUserInput` -- a tie that includes at least one team with fewer than 9 conference games (constructed synthetically, since 2026's real Big Ten schedule is balanced at 9), verifying cumulative-opponent-win-pct compares winning PERCENTAGE regardless of opponent count differences (per the Big Ten's explicit unbalanced-schedule clause), then continues to survive every step and bottoms out at NeedsUserInput with `reason.code === 'ranking-step'`.
  </action>
  <acceptance_criteria>
    - Every fixture's expected result is documented in an adjacent hand-derivation comment, independent of running the engine
    - `bigTenRestartVsContinueDivergence`'s comment explicitly states both the incorrect (continue-only) and correct (restart) answers
    - `bigTenUnbalancedScheduleAndNeedsUserInput`'s comment shows the percentage-based (not raw-count-based) comparison explicitly
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/tiebreakers-bigten.test.ts</automated>
  </verify>
  <done>All 8 named fixtures exist, exported, and hand-derivation comments are present for each; the file compiles cleanly against Task 2's test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Big Ten test suite asserting outcomes and full trace content</name>
  <files>tests/tiebreakers-bigten.test.ts</files>
  <read_first>
    tests/fixtures/tiebreakers/bigten.fixtures.ts (Task 1 output)
    tests/tiebreakers-sec.test.ts (Plan 03-04 -- structural pattern to mirror for consistency across conference test files, if it lands first; otherwise tests/schemas.test.ts)
    shared/domain/tiebreakers/engine.ts (resolveConferenceChampionship's exact signature, Plan 03-03)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-12 -- full trace content assertion requirement)
  </read_first>
  <action>
    Create `tests/tiebreakers-bigten.test.ts` importing every fixture from `./fixtures/tiebreakers/bigten.fixtures`. For each of the 8 fixtures, call `resolveConferenceChampionship('Big Ten', fixture.conferenceGames, fixture.outcomes, fixture.teamIds)` and assert the outcome matches Task 1's hand-derived expectation. For `bigTenUnbalancedScheduleAndNeedsUserInput`, assert the FULL trace content per D-12 (every cycle's `tiedTeams`, every step's `separated`/`values`), not just `status`/`reason.code`. For `bigTenRestartVsContinueDivergence`, assert both the correct positive result and explicitly rule out the incorrect continue-only result.
  </action>
  <acceptance_criteria>
    - All 8 fixtures have a corresponding `it(...)` block
    - `bigTenUnbalancedScheduleAndNeedsUserInput`'s test asserts trace content at the cycle and step level
    - `bigTenRestartVsContinueDivergence`'s test has both a positive and a negative assertion
    - `pnpm test -- tests/tiebreakers-bigten.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/tiebreakers-bigten.test.ts</automated>
  </verify>
  <done>All acceptance criteria pass; 8 passing test cases exist, one per D-10 fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new in this plan's scope | Fixture/test-only plan; consumes engine.ts's already-validated entry point (Plan 03-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-00f | Tampering (silent wrong-answer risk, not an external attacker) | bigten.fixtures.ts hand-derivation accuracy | medium | mitigate | Every fixture's expected result is documented via an independent hand-derivation comment (Task 1), and D-11's coverage gate (Plan 03-08) verifies these fixtures exercise the Big Ten's branch coverage in steps.ts/engine.ts |
</threat_model>

<verification>
`pnpm test -- tests/tiebreakers-bigten.test.ts` exits 0.
</verification>

<success_criteria>
The Big Ten's published procedure (PITFALLS.md's "Conference Tiebreaker Specification Source") is demonstrated correct end-to-end via `resolveConferenceChampionship('Big Ten', ...)` across the full D-10 fixture matrix, with full trace content verified for the NeedsUserInput case per D-12.
</success_criteria>

## Artifacts this phase produces (this plan)

- `tests/fixtures/tiebreakers/bigten.fixtures.ts` -- `bigTenTwoWayTie`, `bigTenThreeWayTie`, `bigTenFourWayTie`, `bigTenFiveWayTie`, `bigTenRestartVsContinueDivergence`, `bigTenPartialHeadToHeadGraph`, `bigTenZeroCommonOpponents`, `bigTenUnbalancedScheduleAndNeedsUserInput`
- `tests/tiebreakers-bigten.test.ts`

<output>
Create `.planning/phases/03-tiebreaker-engine/03-05-SUMMARY.md` when done
</output>
