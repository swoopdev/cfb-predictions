---
phase: 03-tiebreaker-engine
plan: 06
type: tdd
wave: 4
depends_on: ["03-03"]
files_modified:
  - tests/fixtures/tiebreakers/big12.fixtures.ts
  - tests/tiebreakers-big12.test.ts
autonomous: true
requirements: [TIE-01, TIE-02, TIE-03, TIE-04]

must_haves:
  truths:
    - "resolveConferenceChampionship('Big 12', ...) returns the correct #1 and #2 seeds for hand-verified 2-, 3-, 4-, and 5-way Big 12 tie fixtures, matching the Big 12's own published procedure including its step (e) total-wins FCS cap"
    - "A dedicated fixture exercises the collective-bucket comparison at the next-highest-placed-common-opponent step (revised D-05), proving the engine compares against a raw-standings-tied bucket of opponents collectively rather than one at a time"
    - "A restart-vs-continue divergence fixture proves the two branches produce different champions, and the engine picks the one the Big 12's 'repeat the tie-breaking procedure' rule dictates"
    - "A Big 12 tie that bottoms out past total-wins returns NeedsUserInput with reason.code === 'ranking-step', citing the SportSource Analytics Team Rating Score step, not a guessed winner"
  artifacts:
    - tests/fixtures/tiebreakers/big12.fixtures.ts
    - tests/tiebreakers-big12.test.ts
  key_links:
    - "big12.fixtures.ts's collective-bucket fixture is the concrete, hand-verified proof of CONTEXT.md's revised D-05/D-06 decision -- it must be constructed so the collective and one-at-a-time comparisons produce mathematically different results, so a future regression to the wrong interpretation would fail this test"
---

<objective>
Prove TIE-01 through TIE-04 for the Big 12 specifically: a full, hand-verified fixture matrix (per D-10) exercising 2-, 3-, 4-, and 5-way ties, a restart-vs-continue divergence, a partial head-to-head graph, a zero-common-opponents case, AND a dedicated collective-bucket fixture proving the revised D-05 decision, asserting full trace content (per D-12) for every case that bottoms out at NeedsUserInput.

Purpose: this is one of four conference-specific correctness gates the phase's success criteria demand. The Big 12 is uniquely important here because CONTEXT.md's D-05/D-06 were REVISED during this phase's own research pass, after the full primary-source PDF (not just a shorter aggregator page) was read and found to affirmatively state the collective-bucket treatment. This plan's fixture is the concrete artifact that locks that corrected understanding in as an executable regression test.
Output: `tests/fixtures/tiebreakers/big12.fixtures.ts` and `tests/tiebreakers-big12.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tiebreaker-engine/03-CONTEXT.md
@.planning/phases/03-tiebreaker-engine/03-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-derived Big 12 fixture matrix, including the collective-bucket case</name>
  <files>tests/fixtures/tiebreakers/big12.fixtures.ts</files>
  <read_first>
    .planning/research/PITFALLS.md (section "Big 12 -- official tiebreaker policy" -- exact steps a-g for both two-team and multi-team ties, verbatim, including the collective-bucket sentence at step c and the FCS win cap at step e; already loaded in this conversation's context above)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-05 and D-06 AS REVISED 2026-08-13 -- read the current text, not any cached/prior version; this is the single most important read for this task)
    .planning/phases/03-tiebreaker-engine/03-RESEARCH.md (section "Flagged finding: Big 12's 'collective bucket' clause is stated, not silent")
    public/data/2026/teams.json (real Big 12 team ids for readable, grounded fixtures)
    shared/domain/tiebreakers/types.ts (Game, TeamId, GameId shapes from Plans 03-01/03-02/03-03)
  </read_first>
  <action>
    Create `tests/fixtures/tiebreakers/big12.fixtures.ts` following the same `.ts` fixture-module format as sec.fixtures.ts (Plan 03-04). Use real Big 12 team ids for readability (Big 12 roster: Arizona 12, Arizona State 9, Baylor 239, BYU 252, Cincinnati 2132, Colorado 38, Houston 248, Iowa State 66, Kansas 2305, Kansas State 2306, Oklahoma State 197, TCU 2628, Texas Tech 2641, UCF 2116, Utah 254, West Virginia 277). Construct and hand-verify, with an inline comment showing the arithmetic per team at each relevant step, for a 9-case matrix (the standard 8 plus one dedicated collective-bucket case):
    1. `big12TwoWayTie` -- 2 teams tied, resolved by head-to-head.
    2. `big12ThreeWayTie` -- 3 teams tied, resolved partway through the procedure, exercising a genuine restart via the "repeat the tie-breaking procedure" clause.
    3. `big12FourWayTie` -- 4 teams tied, with at least one step producing a 2-2 split.
    4. `big12FiveWayTie` -- 5 teams tied, deep enough to exercise 2+ restart cycles.
    5. `big12RestartVsContinueDivergence` -- constructed so incorrectly continuing after a partial separation produces a DIFFERENT #1/#2 pair than the correct restart; the comment must state both answers.
    6. `big12PartialHeadToHeadGraph` -- 3 tied teams where not every pair played each other, exercising the "one beat all others -> removed, revert to beginning" clause literally.
    7. `big12ZeroCommonOpponents` -- 2+ tied teams with zero conference opponents in common, verifying the walk falls through cleanly with no NaN.
    8. `big12CollectiveBucketComparison` -- THE KEY FIXTURE for this conference: 3 tied teams whose next-highest-placed common opponent, when walking down the frozen base ordering, lands on a bucket containing 2+ raw-standings-tied opponent teams. Construct the win/loss numbers for that opponent bucket so that comparing each tied team's win percentage against the bucket COLLECTIVELY (summed wins / summed games across the whole bucket) produces a DIFFERENT winner than comparing against the bucket's members one opponent at a time -- this is the concrete, mathematically-distinguishing proof of CONTEXT.md's revised D-05. The hand-derivation comment must show both the collective result and the one-at-a-time result explicitly, and state that the collective result is correct per the Big 12 PDF's verbatim "use each team's win percentage against the collective tied teams as a group... rather than the performance against individual tied teams" clause.
    9. `big12NeedsUserInputViaTotalWins` -- a tie surviving head-to-head, common-opponents, next-highest-placed-common-opponent, AND total-wins (with the FCS cap applied) all failing to separate, so the expected result is NeedsUserInput with `reason.code === 'ranking-step'`. Include at least one team with 2+ wins against a non-FBS (not in teams.json) opponent id in this fixture's non-conference/overall games, to prove the FCS cap (via Plan 03-01's deriveOverallWinCount) is actually exercised, not just theoretically wired.
  </action>
  <acceptance_criteria>
    - Every fixture's expected result is documented in an adjacent hand-derivation comment, independent of running the engine
    - `big12CollectiveBucketComparison`'s comment explicitly shows both the collective and one-at-a-time numeric results and states they differ
    - `big12NeedsUserInputViaTotalWins`'s comment shows the FCS-capped win count explicitly (capped value, not the raw uncapped count)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/tiebreakers-big12.test.ts</automated>
  </verify>
  <done>All 9 named fixtures exist, exported, and hand-derivation comments are present for each; the file compiles cleanly against Task 2's test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Big 12 test suite asserting outcomes, the collective-bucket regression, and full trace content</name>
  <files>tests/tiebreakers-big12.test.ts</files>
  <read_first>
    tests/fixtures/tiebreakers/big12.fixtures.ts (Task 1 output)
    tests/tiebreakers-sec.test.ts (Plan 03-04 -- structural pattern to mirror, if it lands first; otherwise tests/schemas.test.ts)
    shared/domain/tiebreakers/engine.ts (resolveConferenceChampionship's exact signature, including the optional allSeasonGames/knownFbsTeamIds parameters for the Big 12's total-wins step, Plan 03-03)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-12 -- full trace content assertion requirement)
  </read_first>
  <action>
    Create `tests/tiebreakers-big12.test.ts` importing every fixture from `./fixtures/tiebreakers/big12.fixtures`. For each of the 9 fixtures, call `resolveConferenceChampionship('Big 12', fixture.conferenceGames, fixture.outcomes, fixture.teamIds, fixture.allSeasonGames, fixture.knownFbsTeamIds)` (the last two arguments only populated where a fixture needs the total-wins step; omit/undefined otherwise) and assert the outcome matches Task 1's hand-derived expectation. For `big12CollectiveBucketComparison` specifically, assert the result matches the COLLECTIVE answer AND explicitly assert it does NOT match the one-at-a-time answer (both a positive and a negative assertion) -- this is the regression test locking in the revised D-05 decision. For `big12NeedsUserInputViaTotalWins`, assert the FULL trace content per D-12, including the `total-wins` step's `values` array showing the FCS-capped counts. For `big12RestartVsContinueDivergence`, assert both the correct positive result and explicitly rule out the incorrect continue-only result.
  </action>
  <acceptance_criteria>
    - All 9 fixtures have a corresponding `it(...)` block
    - `big12CollectiveBucketComparison`'s test has both a positive assertion (collective/correct) and a negative assertion (rules out one-at-a-time)
    - `big12NeedsUserInputViaTotalWins`'s test asserts trace content at the cycle and step level, including the total-wins step's FCS-capped values
    - `pnpm test -- tests/tiebreakers-big12.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/tiebreakers-big12.test.ts</automated>
  </verify>
  <done>All acceptance criteria pass; 9 passing test cases exist, one per fixture, including the collective-bucket regression proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new in this plan's scope | Fixture/test-only plan; consumes engine.ts's already-validated entry point (Plan 03-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-00g | Tampering (silent wrong-answer risk, not an external attacker) | big12.fixtures.ts hand-derivation accuracy, especially the collective-bucket case | high | mitigate | The collective-bucket fixture is constructed to mathematically distinguish the two candidate interpretations (Task 1) and both are asserted (positive + negative) in Task 2 -- this is the highest-severity fixture in the phase since it directly encodes a decision that was already revised once during research after a fuller primary-source read |
</threat_model>

<verification>
`pnpm test -- tests/tiebreakers-big12.test.ts` exits 0.
</verification>

<success_criteria>
The Big 12's published procedure (PITFALLS.md's "Conference Tiebreaker Specification Source") is demonstrated correct end-to-end via `resolveConferenceChampionship('Big 12', ...)` across the full fixture matrix, with the revised D-05 collective-bucket behavior proven by a mathematically-distinguishing fixture and full trace content verified for the NeedsUserInput case per D-12.
</success_criteria>

## Artifacts this phase produces (this plan)

- `tests/fixtures/tiebreakers/big12.fixtures.ts` -- `big12TwoWayTie`, `big12ThreeWayTie`, `big12FourWayTie`, `big12FiveWayTie`, `big12RestartVsContinueDivergence`, `big12PartialHeadToHeadGraph`, `big12ZeroCommonOpponents`, `big12CollectiveBucketComparison`, `big12NeedsUserInputViaTotalWins`
- `tests/tiebreakers-big12.test.ts`

<output>
Create `.planning/phases/03-tiebreaker-engine/03-06-SUMMARY.md` when done
</output>
