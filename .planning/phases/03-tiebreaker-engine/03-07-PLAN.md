---
phase: 03-tiebreaker-engine
plan: 07
type: tdd
wave: 4
depends_on: ["03-03"]
files_modified:
  - tests/fixtures/tiebreakers/acc.fixtures.ts
  - tests/tiebreakers-acc.test.ts
autonomous: true
requirements: [TIE-01, TIE-02, TIE-03, TIE-04]

must_haves:
  truths:
    - "resolveConferenceChampionship('ACC', ...) returns the correct #1 and #2 seeds for hand-verified 2-, 3-, 4-, and 5-way ACC tie fixtures, matching the ACC's own published procedure (as amended July 1, 2026)"
    - "A dedicated fixture proves the ACC's non-win-pct tie definition: an 8-game team and a 9-game team with the same win count (different win percentages) are correctly included in the same tied group, per the ACC's 'alternate number of Conference games and have either the same number of Conference wins or the same number of Conference losses' clause"
    - "A restart fixture proves the ACC's tied-team definition is RE-DERIVED (not reused) on every restart cycle, potentially pulling in a different team than the previous cycle's group"
    - "Since head-to-head is the only computable ACC step, any ACC tie not settled by head-to-head returns NeedsUserInput with reason.code === 'ranking-step', citing the SportSource Analytics Team Success Ranking step -- this is the COMMON case for the ACC, not a rare edge case"
  artifacts:
    - tests/fixtures/tiebreakers/acc.fixtures.ts
    - tests/tiebreakers-acc.test.ts
  key_links:
    - "acc.fixtures.ts's mixed-8/9-game fixture is grounded in the REAL, live 2026 ACC schedule (Boston College, Clemson, Florida State, Georgia Tech, North Carolina play 8 conference games; the other 12 ACC teams play 9), confirmed directly against public/data/2026/games.json during this phase's research -- this is not a hypothetical scenario"
---

<objective>
Prove TIE-01 through TIE-04 for the ACC specifically: a full, hand-verified fixture matrix (per D-10) exercising 2-, 3-, 4-, and 5-way ties, a restart-vs-continue divergence, a partial head-to-head graph, and a zero-common-opponents case, PLUS a dedicated fixture proving the ACC's structurally different (non-win-pct) tied-team definition and its per-restart redefinition, asserting full trace content (per D-12) for every case that bottoms out at NeedsUserInput.

Purpose: this is the highest-stakes conference for TIE-02's "non-percentage tie definition" requirement. PITFALLS.md's own summary states plainly: "head-to-head is the only step this app can compute" for the ACC -- so this conference exercises the NeedsUserInput path far more than the other three, and getting `defineAccTiedTeams` (Plan 03-03, acc.ts) wrong here silently produces the wrong championship participants for real 2026 ACC scenarios given the confirmed-live mixed 8/9-game schedule.
Output: `tests/fixtures/tiebreakers/acc.fixtures.ts` and `tests/tiebreakers-acc.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tiebreaker-engine/03-CONTEXT.md
@.planning/phases/03-tiebreaker-engine/03-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-derived ACC fixture matrix, including the mixed-8/9-game tied-team-definition case</name>
  <files>tests/fixtures/tiebreakers/acc.fixtures.ts</files>
  <read_first>
    .planning/research/PITFALLS.md (section "ACC -- as amended July 1, 2026" -- the exact "Defining Tied Teams" Steps 1(a)/1(b)/1(c), the two-team and three-plus-team procedures, and the "each step restarts, including the definition of tied teams" clause repeated after every sub-step, verbatim; already loaded in this conversation's context above; also Pitfall 3's full worked explanation)
    .planning/phases/03-tiebreaker-engine/03-RESEARCH.md (section "Grounding" -- the live-data-confirmed ACC 8-vs-9-game team split, and section "ACC's defineTiedTeams" pseudocode)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-09 -- every cycle records its own explicit tied-team list, applies to the ACC most meaningfully)
    public/data/2026/teams.json and public/data/2026/games.json (real ACC team ids and the actual confirmed 8-game vs 9-game team split)
    shared/domain/tiebreakers/types.ts (Game, TeamId, GameId shapes from Plans 03-01/03-02/03-03)
  </read_first>
  <action>
    Create `tests/fixtures/tiebreakers/acc.fixtures.ts` following the same `.ts` fixture-module format as sec.fixtures.ts (Plan 03-04). Use real ACC team ids for readability (ACC roster, confirmed 8-game teams: Boston College 103, Clemson 228, Florida State 52, Georgia Tech 59, North Carolina 153; confirmed 9-game teams: California 25, Duke 150, Louisville 97, Miami 2390, NC State 152, Pittsburgh 221, SMU 2567, Stanford 24, Syracuse 183, Virginia 258, Virginia Tech 259, Wake Forest 154). Construct and hand-verify, with an inline comment showing the arithmetic per team at each relevant step, for a 9-case matrix:
    1. `accTwoWayTie` -- 2 teams tied and they played each other, resolved by head-to-head.
    2. `accThreeWayTie` -- 3 teams tied where all are common opponents of each other (the "i. If all the Tied Teams are common opponents" branch), resolved via best record among the tied teams.
    3. `accFourWayTie` -- 4 teams tied, not all common opponents of each other (the "ii. If all the Tied Teams are not common opponents" branch), exercising the simultaneous defeated-everyone/lost-to-everyone removal.
    4. `accFiveWayTie` -- 5 teams tied, deep enough that after head-to-head partially separates, the redefinition (Task's key case, see #5) is exercised at least once more within the same scenario.
    5. `accRestartRedefinesTiedGroup` -- THE KEY FIXTURE for this conference: constructed so that after the first cycle's head-to-head step removes one team, re-running "Defining Tied Teams" (Steps 1(a)/1(b)) on the remaining pool pulls in a DIFFERENT team than was in the original tied group (a team that was NOT part of cycle 1's tied set, because its win percentage only became the new "best" after the cycle-1 removal, or because its alternate-schedule wins/losses now match the new best group). The comment must state explicitly which team enters newly in cycle 2 and why it was excluded from cycle 1.
    6. `accMixedScheduleTiedTeamDefinition` -- an 8-game ACC team (e.g. Florida State or Clemson) and a 9-game ACC team with the SAME win count but different win percentages (e.g. 7-1 at 8 games = .875 vs 7-2 at 9 games = .778, the exact example RESEARCH.md's own fixture-shape suggestion gives) -- assert both are included in the tied group despite the win-pct difference, per the ACC's "alternate number of Conference games and have either the same number of Conference wins or the same number of Conference losses" clause. This is the direct, confirmed-live-for-2026 proof of TIE-02's "unbalanced schedule handling."
    7. `accPartialHeadToHeadGraph` -- 3 tied teams where not every pair played each other, exercising the "ii. not common opponents" branch's defeated-all/lost-to-all predicates.
    8. `accZeroCommonOpponents` -- not directly applicable to the ACC's own step list (it only has head-to-head), so instead construct 2 tied teams that did NOT play each other at all -- assert this correctly produces NeedsUserInput immediately (head-to-head cannot apply with zero shared games), with `reason.code === 'ranking-step'`.
    9. `accNeedsUserInputTypicalCase` -- 2 teams tied that did not play each other (or 3+ teams where head-to-head doesn't fully separate), demonstrating that NeedsUserInput is the COMMON, not rare, outcome for the ACC -- assert full trace shows only the `head-to-head` step attempted (per CONFERENCE_RULES['ACC'].twoTeamSteps/.multiTeamSteps === ['head-to-head'] only, Plan 03-03) before terminating.
  </action>
  <acceptance_criteria>
    - Every fixture's expected result is documented in an adjacent hand-derivation comment, independent of running the engine
    - `accMixedScheduleTiedTeamDefinition`'s comment explicitly shows both teams' win/loss/gamesPlayed/winPct values and states why they are tied despite differing win percentages
    - `accRestartRedefinesTiedGroup`'s comment explicitly names the newly-entering team and the reason it wasn't in cycle 1's group
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/tiebreakers-acc.test.ts</automated>
  </verify>
  <done>All 9 named fixtures exist, exported, and hand-derivation comments are present for each; the file compiles cleanly against Task 2's test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ACC test suite asserting outcomes, the mixed-schedule regression, and full trace content</name>
  <files>tests/tiebreakers-acc.test.ts</files>
  <read_first>
    tests/fixtures/tiebreakers/acc.fixtures.ts (Task 1 output)
    tests/tiebreakers-sec.test.ts (Plan 03-04 -- structural pattern to mirror, if it lands first; otherwise tests/schemas.test.ts)
    shared/domain/tiebreakers/engine.ts and shared/domain/tiebreakers/acc.ts (resolveConferenceChampionship and defineAccTiedTeams signatures, Plan 03-03)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-12 -- full trace content assertion requirement)
  </read_first>
  <action>
    Create `tests/tiebreakers-acc.test.ts` importing every fixture from `./fixtures/tiebreakers/acc.fixtures`. For each of the 9 fixtures, call `resolveConferenceChampionship('ACC', fixture.conferenceGames, fixture.outcomes, fixture.teamIds)` and assert the outcome matches Task 1's hand-derived expectation. For `accMixedScheduleTiedTeamDefinition`, additionally assert directly against `defineAccTiedTeams` (imported from `shared/domain/tiebreakers/acc.ts`) with the fixture's records/baseOrdering, confirming both the 8-game and 9-game team ids are present in the returned tied-team list -- this is a direct unit-level regression, not just an end-to-end assertion, since a bug here could otherwise be masked by a coincidentally-correct downstream step. For `accRestartRedefinesTiedGroup`, assert the FULL trace content per D-12: cycle 1's `tiedTeams` list does NOT include the newly-entering team, and cycle 2's `tiedTeams` list DOES include it -- this directly proves D-09's per-cycle tied-team-list requirement is meaningful for the ACC specifically (the other 3 conferences' cycles always narrow the same starting group; only the ACC's actually changes membership). For `accNeedsUserInputTypicalCase`, assert the trace contains exactly one cycle with exactly one step (`head-to-head`), proving the ACC's genuinely thin computable-step list is respected, not silently padded with steps that will never run.
  </action>
  <acceptance_criteria>
    - All 9 fixtures have a corresponding `it(...)` block
    - `accMixedScheduleTiedTeamDefinition`'s test directly calls `defineAccTiedTeams` and asserts both differently-scheduled teams appear in its output
    - `accRestartRedefinesTiedGroup`'s test asserts cycle-1 vs. cycle-2 `tiedTeams` membership differs by the specific team named in Task 1's fixture comment
    - `accNeedsUserInputTypicalCase`'s test asserts the trace has exactly 1 cycle with exactly 1 step
    - `pnpm test -- tests/tiebreakers-acc.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- tests/tiebreakers-acc.test.ts</automated>
  </verify>
  <done>All acceptance criteria pass; 9 passing test cases exist, one per fixture, including the mixed-schedule and restart-redefinition regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new in this plan's scope | Fixture/test-only plan; consumes engine.ts's/acc.ts's already-validated entry points (Plan 03-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-00h | Tampering (silent wrong-answer risk, not an external attacker) | acc.fixtures.ts hand-derivation accuracy, especially the mixed-8/9-game and restart-redefinition cases | high | mitigate | Both key fixtures are grounded in the real, confirmed-live 2026 ACC schedule and directly unit-test `defineAccTiedTeams` in addition to the end-to-end result (Task 2), since this is the conference with the most structurally different (and highest-risk) tied-team logic in the phase |
</threat_model>

<verification>
`pnpm test -- tests/tiebreakers-acc.test.ts` exits 0.
</verification>

<success_criteria>
The ACC's published procedure (PITFALLS.md's "Conference Tiebreaker Specification Source," as amended July 1, 2026) is demonstrated correct end-to-end via `resolveConferenceChampionship('ACC', ...)` across the full fixture matrix, with the non-win-pct tied-team definition and per-restart redefinition both proven against real 2026 ACC schedule data, and full trace content verified for the NeedsUserInput case per D-12.
</success_criteria>

## Artifacts this phase produces (this plan)

- `tests/fixtures/tiebreakers/acc.fixtures.ts` -- `accTwoWayTie`, `accThreeWayTie`, `accFourWayTie`, `accFiveWayTie`, `accRestartRedefinesTiedGroup`, `accMixedScheduleTiedTeamDefinition`, `accPartialHeadToHeadGraph`, `accZeroCommonOpponents`, `accNeedsUserInputTypicalCase`
- `tests/tiebreakers-acc.test.ts`

<output>
Create `.planning/phases/03-tiebreaker-engine/03-07-SUMMARY.md` when done
</output>
