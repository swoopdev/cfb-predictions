---
phase: 03-tiebreaker-engine
plan: 08
type: execute
wave: 5
depends_on: ["03-04", "03-05", "03-06", "03-07"]
files_modified:
  - vitest.config.ts
autonomous: true
requirements: [TIE-01, TIE-02]

must_haves:
  truths:
    - "The full test suite (all 7 tiebreaker test files plus Phase 1's existing tests) passes with exit code 0"
    - "shared/domain/tiebreakers/** meets a 90% coverage threshold on statements, branches, functions, and lines, enforced by vitest's coverage config (D-11), not just eyeballed"
  artifacts:
    - vitest.config.ts
  key_links:
    - "The coverage threshold is scoped ONLY to shared/domain/tiebreakers/** (a directory-glob threshold), not a global project threshold -- it must not fail the build for unrelated future code with lower coverage"
---

<objective>
Add D-11's per-directory coverage threshold to `vitest.config.ts` and run the full test suite as the phase's final gate, once every conference's fixture matrix (Plans 03-04 through 03-07) exists to give an accurate coverage measurement. Deliberately sequenced as the last wave so the coverage number reflects the complete fixture matrix, not a partial one.

Purpose: D-11 requires this coverage gate to be added in this phase, not deferred -- cheap insurance against an untested branch (e.g. an unreachable restart path, or the collective-bucket vs. one-at-a-time branch) that the hand-written fixture list happens to miss, per PROJECT.md's framing of this phase as the highest-risk, highest-recovery-cost component in the app.
Output: an updated `vitest.config.ts` with a passing coverage gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tiebreaker-engine/03-CONTEXT.md
@.planning/phases/03-tiebreaker-engine/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-11 coverage threshold + full-suite verification</name>
  <files>vitest.config.ts</files>
  <read_first>
    vitest.config.ts (current 4-line file -- extend in place, do not restructure)
    .planning/phases/03-tiebreaker-engine/03-RESEARCH.md (section "vitest.config.ts -- D-11's per-directory coverage threshold" -- the exact Code Example)
    .planning/phases/03-tiebreaker-engine/03-CONTEXT.md (D-11)
    package.json (confirm @vitest/coverage-v8 is already a devDependency -- it is, added during Phase 1 scaffolding; no install needed)
  </read_first>
  <action>
    Extend the existing `test` object in `vitest.config.ts` in place (per the file's own established "incremental-edit" convention -- do not replace the whole file) by adding a sibling `coverage` key: `provider: 'v8'`, `include: ['shared/**/*.ts']`, `exclude: ['shared/**/*.d.ts']`, `thresholds: { 'shared/domain/tiebreakers/**': { statements: 90, branches: 90, functions: 90, lines: 90 } }`. Leave any global/project-wide threshold unset (D-11's gate is scoped ONLY to `shared/domain/tiebreakers/**`, not the whole project, since most of the app doesn't exist yet). Add a code comment above the `coverage` block citing D-11 and noting `branches` is the metric most worth holding to a high bar here, since the restart-vs-continue split (Pitfall 1) and the indeterminate-vs-record split (Pitfall 4) are both literal branches a fixture gap would leave silently untested.

    Run the full suite with coverage (`pnpm test -- --coverage`) and confirm it passes. If any threshold is not met, do NOT lower the threshold to make it pass -- instead identify the specific uncovered branch/statement/function reported in the coverage output and add the minimal additional fixture case (in the relevant Plan 03-04/05/06/07 conference fixture file, or Plan 03-01/02/03's own test files) needed to exercise it, per D-10's "the full matrix is worth the upfront cost" framing. Document any such addition in this plan's SUMMARY.
  </action>
  <acceptance_criteria>
    - `vitest.config.ts` contains a `coverage.thresholds['shared/domain/tiebreakers/**']` entry with statements/branches/functions/lines all set to 90
    - `pnpm test` (full suite, no coverage) exits 0
    - `pnpm test -- --coverage` exits 0 with no threshold violation reported for `shared/domain/tiebreakers/**`
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --coverage</automated>
  </verify>
  <done>vitest.config.ts's coverage block is committed, the full test suite passes, and the 90% threshold on shared/domain/tiebreakers/** is met without any threshold being lowered to force a pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new in this plan's scope | Build/test configuration only; no runtime code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-00i | Tampering (a coverage gate quietly weakened to force a pass) | vitest.config.ts thresholds | medium | mitigate | This plan's action explicitly forbids lowering the threshold to pass -- any gap must be closed with an additional fixture case instead, and any such addition must be documented in the SUMMARY for reviewability |
</threat_model>

<verification>
`pnpm test -- --coverage` exits 0, and the coverage report shows `shared/domain/tiebreakers/**` at or above 90% on all four metrics.
</verification>

<success_criteria>
The phase's D-11 coverage gate is live and passing without having been weakened. The full test suite (Phase 1's existing tests plus this phase's 7 new tiebreaker test files) is green.
</success_criteria>

## Artifacts this phase produces (this plan)

- `vitest.config.ts` (extended) -- `coverage` block with `provider: 'v8'` and the `shared/domain/tiebreakers/**` threshold entry

<output>
Create `.planning/phases/03-tiebreaker-engine/03-08-SUMMARY.md` when done
</output>
