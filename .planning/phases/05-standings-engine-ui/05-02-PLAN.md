---
phase: 05-standings-engine-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/components/StandingsTable.vue
  - app/components/StandingsSidebar.vue
  - app/pages/week/[week].vue
  - app/app.css
autonomous: true
requirements:
  - STAND-01
  - STAND-02
  - STAND-04

must_haves:
  truths:
    - Standings sidebar displays all 4 P4 conferences (SEC, Big Ten, Big 12, ACC) when no filter is active, stacked vertically in the sidebar
    - When a conference filter is active (Phase 2's existing query param), only that conference's standings are displayed in the sidebar
    - Sidebar collapses on mobile devices via a hamburger toggle, expanding/collapsing the standings panel
    - Teams with identical records appear with matching rank numbers, visually indicating they are tied (no badges or icons needed per D-05/D-06)
    - Sidebar text and table contrast hold up in both light and dark themes per CLAUDE.md design constraint
    - Sidebar layout is responsive: desktop width appropriate, mobile drawer/toggle functional, no text truncation of team names

  artifacts:
    - `app/components/StandingsSidebar.vue` — New wrapper component managing multi-conference display logic (D-02: filter-aware, all-vs-single-conference rendering)
    - `app/components/StandingsTable.vue` — Updated to accept optional className/styling props for responsive table styling
    - `app/pages/week/[week].vue` — Updated to use StandingsSidebar component, wire filter query param to standings display logic
    - `app/app.css` — Sidebar responsive layout styles (width, media queries, toggle styling, contrast verification)
    - Responsive design patterns for desktop sidebar (pinned right panel) and mobile drawer (hamburger toggle)

  key_links:
    - `StandingsSidebar` orchestrates multi-conference rendering based on active conference filter (read from URL query param)
    - Conference filter from Phase 2 (existing `?conf=SEC` query param) drives standings sidebar display logic
    - Same `computeStandings()` from Plan 01 produces standings for all 4 conferences; Plan 02 filters the display

---

<objective>
Extend standings display to all 4 P4 conferences with filter-aware rendering, responsive mobile collapse, and visual polish for tie indication and cross-theme contrast.

**Purpose:** Users see the complete conference standings picture (all 4 or filtered to one), with a responsive UI that works on desktop and mobile, completing the MVP standings feature set.

**Output:** Multi-conference standings sidebar with filter integration, responsive mobile collapse, visual tie indication via matching rank numbers, production-ready styling and contrast.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md (design constraints: contrast, team color sparingly as accents)
@.planning/REQUIREMENTS.md (STAND-01, STAND-02, STAND-04)
@.planning/phases/05-standings-engine-ui/05-CONTEXT.md (Decisions D-01 through D-14, especially D-01/D-02 sidebar placement, D-04/D-05/D-06 tie indication, Claude's Discretion on mobile collapse mechanism and styling)
@.planning/phases/02-foundation-read-only-slate/02-CONTEXT.md (conference filter query param structure, existing filter patterns)
@app/pages/week/[week].vue (existing filter query param usage)
@app/components/StandingsTable.vue (from Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StandingsSidebar wrapper component with filter-aware multi-conference rendering (D-02, STAND-01)</name>
  <files>app/components/StandingsSidebar.vue, app/pages/week/[week].vue</files>
  <action>
    Create a new component `StandingsSidebar.vue` that orchestrates rendering of 1-4 conferences' standings based on the active filter:

    **Props:**
    - `standings: StandingsResult` — output from `computeStandings()` (all 4 conferences)
    - `activeConference: string | null` — current conference filter value (e.g., "SEC", "Big Ten", or null for "All")

    **Logic (D-02):**
    - If `activeConference` is null (no filter, or "All" filter): render all 4 conference standings tables stacked vertically within the sidebar
      - Order: SEC, Big Ten, Big 12, ACC (alphabetical or user-preferred, per Claude's discretion)
      - Each conference rendered as a `StandingsTable` with `conferenceName` prop
      - Sidebar is scrollable if content exceeds viewport height
    - If `activeConference` is a specific conference (e.g., "SEC"): render only that conference's standings
      - Single table takes up the full sidebar width, no scrolling needed for typical screens

    **Integration in week/[week].vue:**
    - Pass the computed `standings` object from Plan 01
    - Extract the active conference filter from route query params: `route.query.conf` (already handled by Phase 2's filter logic)
    - Pass `activeConference` prop to StandingsSidebar
    - StandingsSidebar handles the branching logic internally; week view doesn't need to know about filtering

    **Constraint:** Filtering is display-only — standings computation from Plan 01 still computes all 4 conferences (not re-filtering at compute time). This keeps the computation pure and reusable.
  </action>
  <verify>
    <automated>grep -c "activeConference\|StandingsTable" app/components/StandingsSidebar.vue && grep -c "StandingsSidebar" app/pages/week/[week].vue && npm run dev -- open http://localhost:3000/week/1?conf=SEC</automated>
  </verify>
  <done>StandingsSidebar component created, accepts activeConference prop, renders all 4 conferences when none selected, renders single conference when filter active, integrated into week view, manual verification shows correct filtering behavior</done>
</task>

<task type="auto">
  <name>Task 2: Implement responsive mobile collapse, visual tie indication, and cross-theme contrast polish (D-01, D-04/D-05/D-06, CLAUDE.md design constraint)</name>
  <files>app/components/StandingsSidebar.vue, app/app.css, app/pages/week/[week].vue</files>
  <action>
    Implement responsive design and visual polish:

    **Mobile Collapse (D-01):**
    - On mobile (media query: `@media (max-width: 768px)`), sidebar is hidden by default
    - Add a hamburger toggle button (or use Nuxt UI's collapsible pattern) to expand/collapse the standings panel
    - Toggle state managed as component-local reactive (e.g., `ref<boolean>`) — no localStorage persistence needed (state is ephemeral per session)
    - Exact mechanism (hamburger menu, slide-out drawer, accordion) left to Claude's discretion using Nuxt UI components (UButton, UCard, UCollapsible, etc.)
    - Desktop: sidebar always visible as a right panel (no toggle needed)

    **Visual Tie Indication (D-04/D-05/D-06):**
    - Tie indication is already implicit from matching rank numbers in the standings table (Plan 01)
    - Verify that multiple teams with the same rank are clearly visually aligned:
      - Same rank number in the "Rank" column
      - Same W-L values in both record columns
      - No badges, no icons, no extra text (per D-05/D-06)
    - CSS styling: ensure rank column is left-aligned and easy to scan, so matching ranks are immediately obvious

    **Cross-Theme Contrast (CLAUDE.md design constraint):**
    - Verify table text (team names, records, ranks) has sufficient contrast against the sidebar background in both light and dark themes
    - Use Nuxt UI's design tokens for background/text colors (no hard-coded color values)
    - Test in browser's dark mode toggle to confirm readability
    - Team names should not be truncated (consider max-width or word-wrap if needed on narrow screens)

    **Sidebar Layout on Desktop:**
    - Right-side panel pinned to the right of the games grid
    - Width appropriate for a standings table (maybe 250-350px, left to Claude's discretion based on content)
    - Subtle border/shadow to separate from games grid (per Claude's discretion on styling)
    - Scrollable independently if content exceeds viewport height

    **CSS Implementation (app/app.css or scoped in StandingsSidebar.vue):**
    - `.standings-sidebar { display: none; }` on mobile (max-width 768px)
    - `.standings-sidebar { display: block; }` on desktop
    - Toggle button visible only on mobile (or use Nuxt UI's UButton with @click to toggle visibility)
    - Table styling for readability: compact but not cramped
  </action>
  <verify>
    <automated>grep -c "mobile\|@media\|hamburger\|toggle" app/components/StandingsSidebar.vue && npm run dev -- then manually test on mobile viewport (375px) and desktop (1200px) for sidebar collapse and expansion, and contrast check in light/dark mode</automated>
  </verify>
  <done>Mobile: sidebar hidden by default, toggle button expands/collapses standings panel; Desktop: sidebar visible as right panel; Tie indication works via matching rank numbers (no badges); Text contrast verified in light and dark themes; Sidebar styling polished with appropriate spacing and borders; Manual E2E verification confirms responsive behavior and contrast</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conference filter (query param) → standings display | User-controlled URL filter; malformed or unknown conference value should not crash sidebar rendering |
| standings data → UI rendering | Standings object from Plan 01 may have partial data or missing conferences; rendering should handle gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | conference filter query param | Low | Mitigate | StandingsSidebar validates activeConference against known P4 conference names before rendering; unknown values treated as null (all conferences) |
| T-05-04 | Information Disclosure | standings sidebar visibility (mobile collapse) | Low | Accept | Standings are public display data; mobile collapse is UX only, does not alter data access |
</threat_model>

<verification>
- All 4 P4 conferences displayed when no filter active
- Single conference displayed when filter active
- Mobile sidebar toggle functional on narrow viewports
- Tie indication visible via matching rank numbers
- Text contrast sufficient in light and dark themes
- Team names not truncated on any viewport size
- Manual E2E: filter toggle (`?conf=SEC` vs no filter) shows correct standings subset
</verification>

<success_criteria>
- Sidebar renders all 4 P4 conference standings stacked vertically on desktop (no filter)
- Sidebar renders single conference when filter active
- Mobile: sidebar hidden by default, hamburger toggle expands/collapses standings
- Desktop: sidebar visible as right panel alongside games grid
- Tied teams indicated by matching rank numbers (e.g., three teams at "2")
- Text contrast passes light and dark theme review
- All test cases pass; manual verification confirms responsive behavior and filter-aware rendering
</success_criteria>

<output>
Create `.planning/phases/05-standings-engine-ui/05-02-SUMMARY.md` when done
</output>
