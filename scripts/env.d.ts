// Placeholder so `tsc -p tsconfig.scripts.json` has at least one input file until
// Plans 02-05 add scripts/fetch-data.ts and scripts/lib/*.ts. Safe to remove once
// real source files exist under scripts/.
export {}
